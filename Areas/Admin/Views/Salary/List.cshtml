@* @using SIMS_FPT.Models
@model IEnumerable<SalaryModel>
@{ ViewData["Title"] = "Salary List"; Layout = "~/Areas/Admin/Views/Shared/_AdminLayout.cshtml"; }

<div class="page-wrapper"><div class="content container-fluid">
    <div class="page-header"><div class="row align-items-center">
        <div class="col"><h3 class="page-title">Salary</h3><ul class="breadcrumb"><li class="breadcrumb-item"><a href="#">Dashboard</a></li><li class="breadcrumb-item active">Salary</li></ul></div>
        <div class="col-auto text-right float-right ml-auto"><a asp-controller="Accounts" asp-action="AddSalary" class="btn btn-primary"><i class="fas fa-plus"></i></a></div>
    </div></div>
    
    <div class="row"><div class="col-sm-12"><div class="card card-table"><div class="card-body"><div class="table-responsive">
        <table class="table table-hover table-center mb-0 datatable">
            <thead><tr><th>ID</th><th>Name</th><th>Gender</th><th>Joining Date</th><th>Amount</th><th>Status</th><th class="text-right">Action</th></tr></thead>
            <tbody>
                @if(Model != null && Model.Any()) {
                    foreach(var item in Model) {
                    <tr>
                        <td>@item.StaffId</td><td>@item.Name</td><td>@item.Gender</td><td>@item.JoiningDate.ToString("dd MMM yyyy")</td><td>$@item.Amount</td>
                        <td><span class="badge badge-@(item.Status=="Paid"?"success":"danger")">@item.Status</span></td>
                        <td class="text-right"><div class="actions">
                            <a asp-controller="Accounts" asp-action="EditSalary" asp-route-id="@item.StaffId" class="btn btn-sm bg-success-light mr-2"><i class="fas fa-pen"></i></a>
                            <a asp-controller="Accounts" asp-action="DeleteSalary" asp-route-id="@item.StaffId" class="btn btn-sm bg-danger-light" onclick="return confirm('Delete?');"><i class="fas fa-trash"></i></a>
                        </div></td>
                    </tr>
                }} else { <tr><td colspan="7" class="text-center">No data</td></tr> }
            </tbody>
        </table>
    </div></div></div></div></div>
</div></div> *@